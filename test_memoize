#!/usr/bin/perl -w
use strict;
use Memoize;
sub fa {
    sleep(1);
    return 42;
}
my $num = shift @ARGV;
print STDERR "Memoizing...\n";
foreach (0 .. $num-1) {
    Memoize::memoize('fa');
}
print STDERR "Running...\n";
foreach (0 .. 1000-1) {
    fa();
}

