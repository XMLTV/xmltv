#!/usr/bin/perl

=pod

=head1 NAME

tv_grab_pt_meo - Grab TV listings for Portugal.

=head1 SYNOPSIS

tv_grab_pt_meo --help

tv_grab_pt_meo [--config-file FILE] --configure [--gui OPTION]

tv_grab_pt_meo [--config-file FILE] [--output FILE] [--days N]
               [--offset N] [--fast] [--quiet] [--debug]

tv_grab_pt_meo --list-channels [--config-file FILE]
                 [--output FILE] [--quiet] [--debug]

=head1 DESCRIPTION

Output TV listings for several channels available in Portugal.

First run B<tv_grab_pt_meo --configure> to choose, which channels you want
to download. Then running B<tv_grab_pt_meo> with no arguments will output
listings in XML format to standard output.

Channel ids will be output in alphanumeric characters.

B<--configure> Prompt for which channels, and write the configuration file.

B<--gui OPTION> Use this option to enable a graphical interface to be used.
OPTION may be 'Tk', or left blank for the best available choice.
Additional allowed values of OPTION are 'Term' for normal terminal output
(default) and 'TermNoProgressBar' to disable the use of Term::ProgressBar.

B<--config-file FILE> Set the name of the configuration file, the
default is B<~/.xmltv/tv_grab_pt_meo.conf>.  This is the file written by
B<--configure> and read when grabbing.

B<--days N> Grab N days.  The default is 7 days.

B<--offset N> Start N days in the future.  The default is to start
from today.

B<--fast> Only fetch summary information for each programme. This is
only title, start/stop times, category, episode number.

B<--output FILE> Write to FILE rather than standard output.

B<--quiet> Suppress the progress messages normally written to standard
error.

B<--debug> Provide more information on progress to standard error to help in
debugging.

B<--list-channels> Output a list (in xmltv format) of all channels that can be fetched.

B<--version> Show the version of the grabber.

B<--help> Print a help message and exit.

=head1 SEE ALSO

L<xmltv(5)>.

=head1 AUTHOR

Rudi Gualter,
based on tv_grab_pt from Bruno Tavares, gawen@users.sourceforge.net,
based on tv_grab_es, from Ramon Roca.

=head1 BUGS

None known.

=cut

######################################################################
# initializations

use warnings;
use strict;
binmode(STDOUT, ":encoding(UTF-8)");
use Getopt::Long;
#use Date::Manip;
use DateTime;
#use Data::Dumper;
use IO::File;
use File::Path;
use File::Basename;
use LWP::UserAgent;
use Encode;
use JSON;

use XMLTV;
use XMLTV::Version "$XMLTV::VERSION";
use XMLTV::Memoize; XMLTV::Memoize::check_argv('XMLTV::Get_nice::post_nice_json');
use XMLTV::ProgressBar;
use XMLTV::Ask;
use XMLTV::Config_file;
use XMLTV::DST;
use XMLTV::Get_nice 0.005067;
# Force headers for MEO API (required)
$XMLTV::Get_nice::ua->default_header('Content-Type' => 'application/json; charset=utf-8');
$XMLTV::Get_nice::ua->default_header('User-Agent' => 'Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:148.0) Gecko/20100101 Firefox/148.0');
$XMLTV::Get_nice::ua->default_header('Accept' => '*/*');
$XMLTV::Get_nice::ua->default_header('Accept-Language' => 'pt-PT,pt;q=0.9,en-US;q=0.8,en;q=0.7');
$XMLTV::Get_nice::ua->default_header('Accept-Encoding' => 'gzip, deflate, br, zstd');
$XMLTV::Get_nice::ua->default_header('Origin' => 'https://www.meo.pt');
$XMLTV::Get_nice::ua->default_header('Referer' => 'https://www.meo.pt/');
$XMLTV::Get_nice::ua->default_header('Connection' => 'keep-alive');
$XMLTV::Get_nice::ua->default_header('Sec-Fetch-Dest' => 'empty');
$XMLTV::Get_nice::ua->default_header('Sec-Fetch-Mode' => 'cors');
$XMLTV::Get_nice::ua->default_header('Sec-Fetch-Site' => 'same-site');
use HTTP::Cookies;
$XMLTV::Get_nice::ua->cookie_jar( HTTP::Cookies->new );

use XMLTV::Mode;
use XMLTV::Capabilities qw/baseline manualconfig cache/;
use XMLTV::Description 'Portugal';

use XMLTV::Usage <<END;
$0: get Portuguese television listings in XMLTV format
To configure: $0 --configure [--config-file FILE] [--gui OPTION]
To grab listings: $0 [--config-file FILE] [--output FILE] [--quiet] [--offset OFFSET] [--days DAYS]
To list channels: $0 --list-channels
END


my $DOMAIN = 'meo.pt';
my $SOURCE_URL = "http://www.$DOMAIN";


# Attributes of the root element in output.
my $HEAD = { 'source-info-url'	 => "$SOURCE_URL/",
			 'source-data-url'	 => "$SOURCE_URL/tv/canais-programacao/guia-tv",
			 'generator-info-name' => 'XMLTV',
			 'generator-info-url'  => 'http://xmltv.org/',
		   };

# default language
my $LANG="pt";


######################################################################
# get options

# Get options,
our ($opt_help, $opt_output,
	$opt_configure, $opt_config_file, $opt_gui, $opt_quiet,
	$opt_list_channels, $opt_offset, $opt_days, $opt_fast,
	$opt_debug);
$opt_quiet  = 0; # default
$opt_days   = 7; # default
$opt_offset = 0; # default
$opt_fast   = 0; # default
$opt_debug  = 0;
GetOptions('help'			=> \$opt_help,
		   'configure'		=> \$opt_configure,
		   'config-file=s'	=> \$opt_config_file,
		   'gui:s'			=> \$opt_gui,
		   'output=s'		=> \$opt_output,
		   'quiet'			=> \$opt_quiet,
		   'list-channels'	=> \$opt_list_channels,
		   'offset=i'		=> \$opt_offset,
		   'days=i'			=> \$opt_days,
		   'fast'			=> \$opt_fast,
		   'debug'			=> \$opt_debug,		 # undocumented option
		  )
		or usage(0);
usage(1) if $opt_help;

##$XMLTV::Get_nice::Delay = 0 if $opt_debug;

XMLTV::Ask::init($opt_gui);


our $first_day = ($opt_offset || 0);
our $last_day  = $first_day + $opt_days;
die 'cannot grab more than one week ahead' if $first_day >= 7 || $last_day > 7;

my $mode = XMLTV::Mode::mode('grab', # default
							 $opt_configure => 'configure',
							 $opt_list_channels => 'list-channels',
							);

# File that stores which channels to download.
my $config_file
  = XMLTV::Config_file::filename($opt_config_file, 'tv_grab_pt_meo', $opt_quiet);

my @config_lines; # used only in grab mode
if ($mode eq 'configure') {
	XMLTV::Config_file::check_no_overwrite($config_file);
	mkpath(dirname($config_file));
}
elsif ( ($mode eq 'grab') || ($mode eq 'list-channels') ) {
	@config_lines = map { Encode::decode("UTF-8", $_, Encode::FB_DEFAULT) } XMLTV::Config_file::read_lines($config_file);
}
else { die }


# Whatever we are doing, we need the channels data.
our @ch_all;						# list of channels in received order
# init session FIRST
init_meo_session();
my ( $r1, $r2 ) = get_channels();
my %channels = %$r1; 				# channel_data
my %channellabels = %$r2;			# channel label->id cross ref
my @channels;						# channels to fetch (from config file)



######################################################################
# write configuration

if ($mode eq 'configure') {
	open(CONF, ">:encoding(utf-8)", $config_file) or die "cannot write to $config_file: $!";

	
	# Ask about each channel.
	my @chs = sort {
		$channels{$a}->{position} <=> $channels{$b}->{position}
	} keys %channels;
	my @names = map { $channels{$_}->{'channel-name'} . " (" . $channels{$_}->{'debug-channel-number'} .")" } @chs;
	my @qs = map { "add channel $_ ?"} @names;
	my @want = ask_many_boolean(1, @qs);
	foreach (@chs) {
		my $w = shift @want;
		warn("cannot read input, stopping channel questions"), last
		  if not defined $w;
		# No need to print to user - XMLTV::Ask is verbose enough.

		# Print a config line, but comment it out if channel not wanted.
		my $name = shift @names;
		my $chid = $channels{$_}->{'id_by_label'};
		print CONF "channel".($w?'=':'!').$chid.(" "x(20-length $chid))."\t\t# ".substr('    '.$channels{$_}->{'debug-channel-number'},-4)." : ".$channels{$_}->{'channel-name'}."\n";
	}

	close CONF or warn "cannot close $config_file: $!";
	say("Finished configuration.");

	exit();
}


######################################################################
# Get the configuration, even if list-channels (so we know what 'format' to use)
die if $mode ne 'grab' and $mode ne 'list-channels';


# Remove leading & trailing spaces
sub trim( $ ) {
	return $_[0] if !defined $_[0];
	$_[0] =~ s/^\s+|\s+$//g;
	return $_[0];
}


# Read configuration
my $line_num = 1;
foreach (@config_lines) {
    ++$line_num;
    next unless defined $_;
    s/^\s+|\s+$//g;          # trim leading/trailing whitespace
    next if $_ eq '';         # skip empty lines

    # Remove inline comments (everything after #)
    s/\s*#.*$//;

    # Parse the channel line
    if (/^channel([=!])(.+)/) {  
        my $ch_xmltv_id = trim($2);   # trim again in case spaces before #
        push @channels, $ch_xmltv_id if $1 eq '=';
    }
    else {
        warn "$config_file:$line_num: bad line\n";
    }
}


######################################################################
# Not configuration, we must be writing something, either full
# listings or just channels.
#
die if $mode ne 'grab' and $mode ne 'list-channels';

# Options to be used for XMLTV::Writer.
my %w_args;
if (defined $opt_output) {
	my $fh = new IO::File(">$opt_output");
	die "cannot write to $opt_output: $!" if not defined $fh;
	binmode($fh, ":encoding(UTF-8)");
	$w_args{OUTPUT} = $fh;
}
$w_args{encoding} = 'UTF-8';
my $writer;
sub start_writing() { ($writer = new XMLTV::Writer(%w_args))->start($HEAD) }


if ($mode eq 'list-channels') {
	start_writing;
	foreach (@ch_all) {
		$_->{'id'} = $_->{'id_by_label'};
		delete $_->{'channel-name'};		# not a valid DTD element
		delete $_->{'callsign'};			# not a valid DTD element
		delete $_->{'id_by_number'};		# not for output
		delete $_->{'id_by_label'};			# not for output
		$writer->write_channel($_)
	}
	$writer->end();
	exit();
}


######################################################################
# We are producing full listings.
die if $mode ne 'grab';

die "No channels specified, run me with --configure\n"
  if not scalar @channels;

start_writing;

# the order in which we fetch the channels matters
#
# This progress bar is for both downloading and parsing.  Maybe
# they could be separate.
#
my $bar = new XMLTV::ProgressBar('getting listings', scalar @channels)
  if not $opt_quiet;

# write the <channels> elements
foreach my $ch_xmltv_id (@channels) {
    next unless defined $ch_xmltv_id && $ch_xmltv_id ne '';

    my $label = xmltv_id_to_label($ch_xmltv_id);
    next unless defined $label && exists $channellabels{$label};

    my $ch = $channels{ $channellabels{$label}->{id} };

    my $channel = {
        'id'           => $ch->{'id_by_label'},
        'display-name' => $ch->{'display-name'},
        'icon'         => $ch->{'icon'},
    };

    $writer->write_channel($channel);
}


# time limits for grab
my $today_date = DateTime->today(time_zone => 'Europe/Lisbon');
my $grab_start = $today_date->epoch() + ($opt_offset * 86400);
my $grab_stop  = $grab_start + ($opt_days * 86400);
print STDERR "\n start/end grab: $grab_start $grab_stop \n" if $opt_debug;

my $dt_start = DateTime->from_epoch( epoch => $grab_start );
my $dt_stop  = DateTime->from_epoch( epoch => $grab_stop );



my $some=0;
foreach my $ch_did (@channels) {
    my $label = xmltv_id_to_label($ch_did);
    unless (defined $label && exists $channellabels{$label}) {
		warn "Skipping unknown channel ID: '$ch_did'\n";
		next;
	}

	foreach (get_programmes($ch_did)) {
		$writer->write_programme($_);
		$some = 1;
	}

	update $bar if $bar;
}
if (not $some) {
  die "no programmes found\n" unless $some;
}

$writer->end();

finish $bar if $bar;


######################################################################
# subroutine definitions

# Use Log::TraceMessages if installed.
BEGIN {
	eval { require Log::TraceMessages };
	if ($@) {
		*t = sub {};
		*d = sub { '' };
	}
	else {
		*t = \&Log::TraceMessages::t;
		*d = \&Log::TraceMessages::d;
		Log::TraceMessages::check_argv();
	}
}

#helper: extract label from xmltv id
sub xmltv_id_to_label {
    my ($id) = @_;
    return undef unless defined $id;
    $id =~ s/^\s+|\s+$//g;  # trim whitespace
    if ($id =~ /^(.+)\.meo\.pt$/i) {
        return $1;            # capture everything before ".meo.pt"
    }
    return undef;            # fallback
}


# Wrapper around Encode (and fix_utf8)
sub toUTF8( $ )  {
     return fix_utf8($_[0]);
}


# UTF-8 fixups.
sub fix_utf8( $ ) {
	return $_[0] if !defined $_[0];

	# there's some UTF-16 codes in the data
	$_[0] =~ s/\x{2013}/–/g;   # en dash
	$_[0] =~ s/\x{20ac}/€/g;   # euro
	$_[0] =~ s/\x{2026}/…/g;   # ellipsis
	$_[0] =~ s/\x{201c}/“/g;
	$_[0] =~ s/\x{201d}/”/g;
	$_[0] =~ s/\x{2039}/‘/g;
	$_[0] =~ s/\x{203a}/’/g;

	return $_[0];
}



sub make_episode_num_from_title {
    my ($title) = @_;
    my ($season, $episode, $match);

    # Master regex list for all variants
    my @regexes = (
        # 1. T/S/Season/Temporada + Ep/Episódio/Episode, optional leading dash and spaces
        qr/(?:-?\s*)?(?:(?:T|S|Season|Temporada)\.?\s*(\d+))\s*(?:-?\s*)?(?:E|Ep\.?|Episódio|Episode)\.?\s*(\d+)/i,

        # 2. Variants with space only (no dash)
        qr/(?:-?\s*)?(?:(?:T|S|Season|Temporada)\.?\s*(\d+))\s+(?:E|Ep\.?|Episódio|Episode)\.?\s*(\d+)/i,

        # 3. SxxExx / SxEx (like S01E01, S1E1)
        qr/S?(\d+)[\s\-]*E(\d+)/i,

        # 4. Episode-only patterns (Ep/Ep./Episódio/Episode)
        qr/(?:-?\s*)?(?:E|Ep\.?|Episódio|Episode)\.?\s*(\d+)/i,
    );

    foreach my $rx (@regexes) {
        if ($title =~ $rx) {
            $match = $&;  # full matched string
            if (defined $2) {      # season + episode
                $season  = $1 || 1;
                $episode = $2;
            } else {                # episode only
                $season  = 1;
                $episode = $1;
            }
            last;
        }
    }

    # Remove only the matched episode/season part
    $title =~ s/\Q$match\E// if $match;

    # Remove trailing dash or spaces left after removal
    $title =~ s/[-–]\s*$//;
    $title = trim($title);

    return ($title, undef) unless defined $episode;

    # XMLTV internal format: (season-1).(episode-1).
    my $output = [ [ ($season-1) . "." . ($episode-1) . ".", 'xmltv_ns' ] ];

    # Onscreen format: "season episode" (space-separated)
    push @$output, [ "$season $episode", 'onscreen' ];

    return ($title, $output);
}

sub get_programmes {
	my ($ch_xmltv_id) = @_;

    t "Getting channel $ch_xmltv_id\n";

    my $label = xmltv_id_to_label($ch_xmltv_id);
    my $ch = $channels{ $channellabels{$label}->{id} };

    my $ch_meo_id =  $ch->{'callsign'};
    print STDERR " CH= $ch_meo_id \n" if $opt_debug;

	my $programmes = {};
	
	# The new API is per-day, so we must loop through the requested range
	my $current_dt = $dt_start->clone();

	while ($current_dt < $dt_stop) {
		
		my $date_str = $current_dt->strftime('%Y-%m-%d');
		
		# New GET URL format
		my $url = "https://meogouser.apps.meo.pt/Services/GridTv/GridTv.svc/GetLiveChannelProgramsByDate?" .
		          "callLetter=$ch_meo_id&date=$date_str&userAgent=IPTV_OFR_GTV";
		
		print STDERR "  URL: $url\n" if $opt_debug;
		
		my $response = $XMLTV::Get_nice::ua->get($url);
		unless ($response->is_success) {
			warn "Failed to fetch programmes for $ch_meo_id on $date_str. Skipping this day.\n";
			$current_dt->add(days => 1);
			next;
		}
		
		my $data = decode_json($response->decoded_content);
		
		my $program_list = $data->{Result};
		
		unless (ref $program_list eq 'ARRAY') {
			warn "($ch_meo_id) : API returned no programme list\n";
			return;
		}
		

		foreach my $prog (@$program_list) {
			next unless exists $prog->{ProgramId};
			
			my ( $p_id, $p_title, $p_category, $p_credits, $p_year, $p_subtitle, $p_length, $p_desc, $p_duration, $p_start, $p_stop, $p_start_epoch, $p_stop_epoch, $p_episode_num, $p_rating );

			$p_id		= $prog->{'ProgramId'};
			$p_title	= $prog->{'Title'};
			$p_desc		= $prog->{'Synopsis'};
			$p_rating   = $prog->{'IsAdultContent'} ? "Adult" : "Non Adult";
			$p_category = $prog->{'Category'} ? $prog->{'Category'} : undef;
			$p_subtitle = $prog->{'Subtitle'} ? $prog->{'Subtitle'} : undef;
			$p_credits  = $prog->{'Credits'} ? $prog->{'Credits'} : undef;
			$p_year     = $prog->{'Year'} ? $prog->{'Year'} : undef;
			
			($p_title, $p_episode_num) = make_episode_num_from_title($p_title);
			
			# Parse ISO-like dates: 2026-03-21T00:35:00
			# We use internal regex to avoid heavy date parsing overhead
			if (my ($y, $m, $d, $h, $i, $s) = $prog->{StartDate} =~ /^(\d+)-(\d+)-(\d+)T(\d+):(\d+):(\d+)$/) {
				my $start_dt = DateTime->new(
					year => $y, month => $m, day => $d, 
					hour => $h, minute => $i, second => $s, 
					time_zone => 'Europe/Lisbon'
				);
				
				# Same for EndDate
				my ($ey, $em, $ed, $eh, $ei, $es) = $prog->{EndDate} =~ /^(\d+)-(\d+)-(\d+)T(\d+):(\d+):(\d+)$/;
				my $end_dt = DateTime->new(
					year => $ey, month => $em, day => $ed, 
					hour => $eh, minute => $ei, second => $es, 
					time_zone => 'Europe/Lisbon'
				);

				$p_start_epoch = $start_dt->epoch();
				$p_stop_epoch  = $end_dt->epoch();
				$p_start       = $start_dt->strftime("%Y%m%d%H%M%S %z");
				$p_stop        = $end_dt->strftime("%Y%m%d%H%M%S %z");
				$p_length 	   = $p_stop_epoch - $p_start_epoch;


				# Filter range
				next if $p_start_epoch < $grab_start || $p_start_epoch >= $grab_stop;

				# Store program
				my %prog_entry = (
					'channel'     => $ch_xmltv_id,
					'id'          => $p_id,
					'title'       => $p_title,
					'desc'        => $p_desc,
					'category'    => $p_category,
					'sub-title'   => $p_subtitle,
					'credits'     => $p_credits,
					'year'        => $p_year,
					'rating'      => $p_rating,
					'start'       => $p_start,
					'stop'        => $p_stop,
					'length'      => $p_length,
					'episode-num' => $p_episode_num,
				);

				# Prevent duplicates
				$programmes->{$ch_xmltv_id}->{$p_start_epoch}->{$p_title} = \%prog_entry;
			}
			
		}
			
		$current_dt->add(days => 1);
		
	}

	# did we get any programmes?
	if ( scalar keys %{$programmes} == 0 ) {
			warn "($ch_xmltv_id) : no programmes found\n";
		return;
	}

	# format the programmes ready for XMLTV::Writer
	my @r;
    foreach my $ch (keys %$programmes) {
        foreach my $start (sort keys %{ $programmes->{$ch} }) {
            foreach my $prog (values %{ $programmes->{$ch}{$start} }) {
                push @r, make_programme_hash($ch, $prog);
            }
        }
    }

	return @r;
}


# reformat the data to something acceptable to xmltv:::writer
sub make_programme_hash {
	my ( $ch_xmltv_id, $cur ) = @_;

	my %prog;

	$prog{channel} = $cur->{'channel'};

	#$prog{channel} =~ s/\s/_/g;

	$prog{'title'} 		= [ [ toUTF8( $cur->{'title'} ), $LANG ] ];
	$prog{'sub-title'}  = [ [ toUTF8( $cur->{'subtitle'} ), $LANG ] ] if defined $cur->{'subtitle'};
    $prog{'category'}   = [ [ toUTF8( $cur->{'category'} ), $LANG ] ] if defined $cur->{'category'};
	$prog{'episode-num'} = $cur->{'episode-num'} if $cur->{'episode-num'};
	$prog{'start'} 		= $cur->{'start'} if $cur->{'start'};
	$prog{'stop'} 		= $cur->{'stop'} if $cur->{'stop'};
	$prog{'length'} 	= $cur->{'length'} if $cur->{'length'};
	$prog{'desc'} 		= [ [ toUTF8( $cur->{'desc'} ), $LANG ] ] if $cur->{'desc'};
	$prog{'icon'} 		= [ { 'src' => $cur->{'icon'} } ] if $cur->{'icon'};
	$prog{'rating'} 	= [ [ $cur->{'rating'}, 'CCE' ] ] if $cur->{'rating'};
	$prog{'credits'}    = $cur->{'credits'} if defined $cur->{'credits'};
    $prog{'date'}       = $cur->{'year'} if defined $cur->{'year'};

	return \%prog;
}


#initiate session 
sub init_meo_session {
    my $url = 'https://meogouser.apps.meo.pt/Services/Authentication/User.svc/IsOauthAuthenticated';

    print STDERR "Initializing MEO session...\n" if $opt_debug;

    my $response = $XMLTV::Get_nice::ua->get($url);

    die "Failed to init session: " . $response->status_line
        unless $response->is_success;

    print STDERR "Session initialized, cookies stored\n" if $opt_debug;
}


# get channel listing
sub get_channels {
	my $bar = new XMLTV::ProgressBar( 'getting list of channels', 1 )  if not $opt_quiet;
	my ( %channels, %channellabels );

	# retrieve channels
	my $url = 'https://meogouser.apps.meo.pt/Services/GridTv/GridTv.svc/GetContentsForChannels?userAgent=IPTV_OFR_GTV';
    print STDERR " URL= $url \n" if $opt_debug;
	t $url;

	my $response = $XMLTV::Get_nice::ua->get($url);
	
	die "failed to fetch channels: " . $response->status_line
        unless $response->is_success;
		

	# fetch json content (already decoded from utf8)
	my $data = decode_json($response->decoded_content);
	

	foreach (@{ $data->{Result} }) {

		my ($channel_id_label, $channel_id_number, $channel_position, $channel_name, $channel_logo);
		
		$channel_id_label	= $_->{CallLetter};
		$channel_id_number	= $_->{Id};
		$channel_position		= $_->{ChannelPosition};
		$channel_name		= $_->{Title};
		$channel_logo		= "https://cdn-er-images.online.meo.pt/api/Channels/logos/image?callLetter=$channel_id_label&profile=corner_transparent_positive&width=60";

		# store the channel
		if ( defined $channel_id_label && $channel_id_label ne '' ) {
			my $ch =
			  {
				'channel-name'  		=> toUTF8($channel_name),
				'display-name'  		=> [ [ toUTF8($channel_name), $LANG ] ],
				'icon'					=> [ { 'src' => $channel_logo } ],
				'position'				=> $channel_position,
				'id'					=> $channel_id_number,
				'callsign'				=> $channel_id_label,
				'id_by_label'			=> $channel_id_label.'.'.$DOMAIN,
				'id_by_number'			=> $channel_id_number.'.'.$DOMAIN,
				'debug-channel-number'	=> $channel_id_number,
			  };
			$channels{$channel_id_number} = $ch;
			$channellabels{$channel_id_label} = { 'id' => $channel_id_number };
			push @ch_all, $ch;
		}

	}

	die "no channels could be found" if not keys %channels;

	update $bar if not $opt_quiet;
	finish $bar if not $opt_quiet;
	return ( \%channels, \%channellabels );
}
